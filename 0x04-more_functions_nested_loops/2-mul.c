#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first argument
 * @b: second argument
 *
 * Description: The function is meant to multiply two integers
 * Return: Always (0) Success
 */
int mul(int a, int b)
{
	return (a * b);
}
